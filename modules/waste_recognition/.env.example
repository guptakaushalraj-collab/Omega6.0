# waste_recognition — environment
#
# Copy to .env and adjust:  cp .env.example .env
# Real environment variables take precedence over this file, so container and
# CI config always wins over a stray local .env.
#
# This module has NO outbound dependencies. Everything below is optional and
# the defaults are correct for local development.

# Port to listen on.
PORT=4102
