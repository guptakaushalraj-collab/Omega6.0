# analytics_dashboard — SOLD ($35,000)
# Push-based: derives everything from events it is sent. No dependencies.
PORT=8004

# Events retained before oldest-first eviction. NOTE: derived metrics reflect
# the retention window, not all time — raise this or mirror to a warehouse if
# you need durable history.
MAX_EVENTS=20000
